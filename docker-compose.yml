version: "3"
services:
  redis:
    image: redis
    ports:
      - "6380:6379"
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
